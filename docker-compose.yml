version: '3.3'

services:
  web:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./core:/app/web
    depends_on:
      - postgres

  postgres:
    image: postgres:12-alpine
    container_name: postgres
    restart: always
    ports: 
      - '5432:5432'